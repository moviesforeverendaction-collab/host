server {
    server_name auth.YOUR_DOMAIN;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/auth.YOUR_DOMAIN/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/auth.YOUR_DOMAIN/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

}
server {
    if ($host = auth.YOUR_DOMAIN) {
        return 301 https://$host$request_uri;
    }


    listen 80;
    server_name auth.YOUR_DOMAIN;
    return 404;


}

# Script End Here

# COMMANDS
# -> sudo nano /etc/nginx/sites-available/auth
# Paste The Codes Provide 👆
# CTRL+X Y Enter
# sudo nginx -t
# sudo systemctl restart nginx
        # Done
        
   # Still Need Help -> @MR_ARMAN_08
   
# Note This Your Flask Url And The nginx Config Url Must Be Same.